1. relative paths are relative to cmakelsists
2. cmak egenerates the build files, ninja is the build system that compiles the code
3. running cmake in its own build keeps the file mess in its own directory